from random import randint
import keyboard
while True:
    if keyboard.is_pressed('q'):  # 'q'キーが押された場合
        print("qキーが押されました")
        break
n=int(input("桁を指定してください:"))
def f():
  while True:
    if keyboard.is_pressed('shift'):
      p=randint(10**(n-1),10**n-1)
      if p%5==0:
        continue
      print(p**3)
    break
  print("\n\n")
  if keyboard.is_pressed('Enter'):
    print(p)
for i in range(int(input())):
  f()